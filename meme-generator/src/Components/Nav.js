const WebLogo = "Images/Troll-Face.svg"

const Nav = () => (
    <nav>
        <img className="logo" src={WebLogo} alt='troll-face'/>
        <p className="logo-title">MemeGenerator</p>
    </nav>
)

export default Nav