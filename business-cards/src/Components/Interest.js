import React from "react"

const Interest = ({body}) => (
    <div className="inerest">
        <h3 className="card-sub-title">Interest</h3>
        <p className="card-sub-body">{body}</p>
    </div>    
)

export default Interest